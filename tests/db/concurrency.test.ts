// DEPRECATED: This file tested the old booking-acceptance RPC which used
// legacy goods-listing coupling on bookings and the 'other_accepted' status.
// All of those were removed in the trips-only reorientation.
//
// This file is kept as a placeholder to avoid breaking test discovery.

import { describe, it } from 'vitest';

describe('(DEPRECATED) booking-acceptance concurrency', () => {
  it.skip('superseded by the trips-only booking model', () => {});
});
